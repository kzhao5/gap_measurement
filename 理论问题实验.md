# (W1′) 轨迹内耦合的测量指导

## 0. 一句话目标

精确解是

$$\log M^\star(k_t,p_t)=\min\Big(\log k_t,\ \underbrace{\log n+\log\big(1-\mathbb E[\textstyle\prod_s m_s]\big)}_{\gamma,\ \text{全轨迹共享}}+\underbrace{\Delta(k_t,p_t)}_{\text{唯一的 }t\text{-通道}}\Big),\qquad
\Delta:=\log\frac{L_1}{L_2},$$

$$L_j(k,p):=\mathbb E\Big[\prod_{s\ne t}m_s^{\,j}\ \Big|\ (k_t,p_t)=(k,p)\Big].$$

**要测的就是 $\Delta$ 对 $\varphi_t$ 的依赖。** 在 (W1) 独立性下 $\Delta$ 与 $(k,p)$ 无关（常数 cap）；$\Delta$ 随 $\varphi_t$ 上升且斜率 $O(1)$，则 $\lambda_+\varphi_t$ 形状的带宽由 $\mathcal R_n$ 直接推出。

**重要（符号）**：不要用直觉猜符号。记 $S_{-t}:=\sum_{s\ne t}\log m_s$，$\mu(\varphi):=\mathbb E[S_{-t}\mid\varphi_t]$，$v(\varphi):=\mathrm{Var}(S_{-t}\mid\varphi_t)$，则

$$\Delta\approx-\mu(\varphi)-\tfrac32 v(\varphi).$$

均值通道与方差通道**符号相反**：轨迹内正相关同时压低 $\mu$（使 $\Delta$ 变大，带变宽）和抬高 $v$（使 $\Delta$ 变小，带变窄）。粗估 $\mu\approx-Q_{-t}/2$、$v\approx Q_{-t}V$，$V\approx1$ 时方差通道占优，净效应为**带变窄**——与目标算子反向。所以必须**直接测 $\Delta$**，$\mu,v$ 只作机理诊断。

---

## 1. 需要 dump 的原始数据

一次 rollout batch，逐 token 一行：

| 字段 | 说明 |
|---|---|
| `traj_id` | 轨迹标识 |
| `pos` | token 位置 $t$ |
| `T` | 该轨迹长度 |
| `logp_old` | $\log\pi_{\mathrm{old}}(y_t\mid h_t)$，训练侧一次 forward |
| `logp_inf` | $\log\pi_{\mathrm{infer}}(y_t\mid h_t)$，推理引擎返回 |

`log_k = logp_old - logp_inf`，`p = exp(logp_old)`。**注意精度**：两个 logprob 必须在同一 dtype 下比较，且 `logp_inf` 要取引擎未经任何后处理的原始值，否则测到的是后处理噪声。

规模：$n\gtrsim2000$ 条轨迹即可，$\Delta$ 的估计误差主要来自轨迹数而非 token 数。

---

## 2. 派生量

```python
import numpy as np

c, eps0, z_plus = 0.156, EPS0_MEASURED, Z_PLUS_REF   # 见 §5 敏感性

phi   = np.maximum(1.0 - p, eps0)
sigma = c * phi
Z     = (log_k + sigma**2 / 2) / sigma

# 当前算子下的 per-token 权重（log 空间，全程不取 exp）
log_C = sigma * z_plus - sigma**2 / 2          # cap 在 k 单位下的对数
log_m = np.minimum(log_k, log_C)
```

`z_plus` 的鸡生蛋问题：$\Delta$ 依赖 $m$，$m$ 依赖阈值。**做法**：取当前生产配置的 $z_+$ 作参考值算一遍，再在 §5 做敏感性扫描。不要试图在这一步做不动点迭代。

---

## 3. 留一和 $S_{-t}$

```python
import pandas as pd
df = pd.DataFrame({'traj': traj_id, 'log_m': log_m, 'phi': phi, 'T': T, 'sigma': sigma})

tot = df.groupby('traj')['log_m'].transform('sum')
df['S_minus'] = tot - df['log_m']          # 关键：留一，不能用整条和
```

留一是必须的：若含 $\log m_t$ 自身，回归会捞到一个平凡的自相关（$\log m_t$ 本身就依赖 $\varphi_t$），$\beta$ 全是假信号。

---

## 4. 主估计量：分桶直接算 $\Delta(\varphi)$

$L_1,L_2$ 是 $e^{S_{-t}}$ 与 $e^{2S_{-t}}$ 的条件期望，$S_{-t}$ 量级可达 $-10^2$，**必须用 logsumexp 且减公共偏移**。

```python
from scipy.special import logsumexp

# 按 phi 分桶（log 均匀，覆盖 eps0 到 1）
bins = np.geomspace(eps0, 1.0, 21)
df['bin'] = np.digitize(df['phi'], bins)

rows = []
for b, g in df.groupby('bin'):
    S  = g['S_minus'].values
    N  = len(S)
    o  = S.mean()                                  # 公共偏移，抵消后不影响 Delta
    logL1 = logsumexp(S     - o)   - np.log(N)
    logL2 = logsumexp(2*S - 2*o)   - np.log(N)
    rows.append(dict(
        bin=b, phi=g['phi'].median(), n=N,
        # Delta 的偏移修正：logL1 含 +o，logL2 含 +2o
        Delta = (logL1 + o) - (logL2 + 2*o),
        mu    = S.mean(),
        var   = S.var(ddof=1),
        T_med = g['T'].median(),
    ))
res = pd.DataFrame(rows)
```

**自检**：`Delta ≈ -mu - 1.5*var` 应在 10% 内成立；若偏离很大，说明 $S_{-t}$ 的条件分布非高斯（重尾），此时以 logsumexp 的直接值为准，并记录该桶的有效样本数 $\mathrm{ESS}=\exp(2\,\mathrm{logL1}-\mathrm{logL2}-\log N)$，ESS 太小的桶不可信。

---

## 5. 两个必须做的对照

**长度混杂。** $S_{-t}$ 的量级 $\propto T$，而 $\varphi_t$ 与 $T$ 可能相关（长轨迹更不确定）。分层复算：按 $T$ 分 3--5 层，层内重做 §4，看 $\Delta(\varphi)$ 的形状是否稳定。若只在合并样本中出现斜率、层内消失，那是长度效应不是耦合效应。

**阈值敏感性。** $z_+\in\{0.5,1,2\}\times z_+^{\mathrm{ref}}$ 各跑一遍。$\Delta$ 的斜率若随 $z_+$ 剧变，说明结论由尾部截断位置驱动，需在论文里如实标注适用范围。

---

## 6. 线性汇总（回归形式）

分桶给形状，回归给一个可写进论文的数。两种设定都跑，因为它们对应不同的算子形状：

```python
import statsmodels.formula.api as smf

# 设定 A：幂律带宽  C ∝ phi^beta   →  log-band ∝ phi^beta
mA = smf.ols('Delta ~ np.log(phi)', data=res, weights=res['n']).fit()

# 设定 B：线性带宽  Delta = a + b*phi  →  与论文算子 lambda_+ * phi 同形
mB = smf.ols('Delta ~ phi',          data=res, weights=res['n']).fit()
```

标准误必须按轨迹聚类（token 不独立）。分桶后可直接对轨迹做 bootstrap：重采样 `traj_id`（整条进出）500 次，重跑 §4 + 回归，取斜率的分位数。

**同时报告机理分解**：把 `mu` 和 `var` 分别对 `phi` 回归，看净效应由哪个通道主导。

---

## 7. 判决表

设定 B 的斜率记为 $b$（单位：$\log k$ 的带宽 per 单位 $\varphi$），与目标 $\lambda_+=cz_+$ 比较。

| 结果 | 含义 | 论文怎么写 |
|---|---|---|
| $b$ 不显著，或 $\|b\|\ll\lambda_+$ | (W1) 在一阶意义上成立，$t$-通道被 FOC 压死 | $\mathcal R_n$ 只定形状（cap）与阈值数值；$\varphi_t$ 的依据留在 $\sigma_t=c\varphi_t$ 的实测与 (Z1) 枢轴性 |
| $b<0$ 且显著 | 方差通道占优，MSE 要求低置信端**更紧** | 如实报告：常数 cap 在序列层面偏松。这是发现，不是失败 |
| $b>0$ 且 $\|b\|\sim\lambda_+$ | 均值通道占优，$\varphi_t$ 由 MSE 推出 | Step 4--5 改写为非独立 FOC，(W1) 降级为被否证的对照 |

**门槛提示**：$\lambda_+=cz_+$，以 $c=0.156$、$z_+\sim10$ 计约为 $1.5$。$|b|<0.1$ 基本可判为"无效应"。

---

## 8. 顺带测的两个量（同一次 pass）

**轨迹级隐尺度 $\omega^2$。** 若存在轨迹级隐变量 $\Lambda$（题目难度、路由整体活跃度），则

```python
per_traj = df.groupby('traj').agg(L=('log_m','sum'), Q=('sigma', lambda s:(s**2).sum()))
omega2_hat = per_traj['L'].var() - per_traj['Q'].mean()   # 超出独立预测的部分
```

$\omega^2>0$ 显著即存在轨迹级尺度波动，可给出一条**独立于 $\varphi_t$** 的序列级修正项，无论 $b$ 是什么符号都有价值。

**通道分离的自相关。** 分别算 $\varphi_t$ 与 $Z_t$ 的轨迹内自相关（lag 1--20）：

- $\varphi$ 通道相关强 → 不确定性成段，走 §7 第二行的机理；
- $Z$ 通道相关强 → 路由异常成簇，$\Delta$ 会依赖 $k$ 而非 $p$，此时应考虑序列级（而非 token 级）修正。

---

## 9. 交付物

1. $\Delta(\varphi)$ 分桶曲线 + bootstrap 置信带（$T$ 分层版本各一张）；
2. 设定 A/B 斜率与聚类标准误，$\mu,v$ 分解；
3. $z_+$ 三档敏感性；
4. $\omega^2$ 点估计与置信区间；
5. $\varphi$ / $Z$ 两通道自相关图。

前两项决定 Section 4 写哪个版本，其余进附录。