# 论文图表与验证实验:实测判读(2026-08-15)

全部图在 `results/figs/paper/`,参数与数字在 `results/paper/params_{moe,dense}.json`、
`fig5_params.json`、`fig1b_params.json`,CSV 同目录;脚本 `analysis/paper_*.py`。
所有数字为实测,预注册判读逐条对照,不符处如实标注。

## 常数(实测)
- 静态 c:MoE **0.155**(等质量 20 桶,加权拟合;文档值 0.156 ✓),dense **0.0354**(文档值 0.035 ✓);ε₀=5e-3;κ=5。
- 训练时 c(champion lgrid_0,lag=0 逐文件):早 0.61 / 中 1.28 / 晚 1.67。

## E-A~E-E 结果(campaign MoE 12.3M / dense 12.4M)

| 实验 | 实测 | 预注册判读是否成立 |
|---|---|---|
| E-A 双标度 | **新鲜数据上核外(\|Z\|>5)中位 \|log k\| 也 ∝ φ**(比主体高 ~6×,斜率≈1);"平坦 0.12 层"**不出现** | ✗(见 Fig 1b 的修正版) |
| E-A' 含陈旧(Fig 1b) | 配对 stale(lag 29)与 champion 训练 token:低置信端核外仍 ∝ φ;**高置信端(φ<0.02)核外 0.04–0.11**(训练随阶段升高),为 cφ 的 10–100× | **仅高置信端成立**——正是带的地板区、90% 截断发生处 |
| E-B 稀疏性 | ε̂(κ=4/5/6)= 2.16% / **1.26%** / 0.78%;corr(δ,φ)=0.004(δ⊥φ ✓);轨迹内弱成簇:acf lag1 0.041(置换零 0.0002),lift lag1 4.2× → lag4 2.6× | ✓ 稀疏、与 φ 独立;**有弱成簇**(定理需显式建模或说明) |
| E-C 符号包络 | 高置信桶核外 **P(log k>0)=0.977**(campaign)/ 1.00(配对、训练);q99 ≤ 0.33 | ✓ 高置信端上侧威胁、有界 |
| E-D 机制 | 核外 vs 硬翻转:P(hf\|out)=0.78 vs P(hf)=0.53(1.46×);核外平均硬翻层数 2.04 vs 1.09 | 弱归因(与 H-var 一致):路由翻转富集但非决定性 |
| E-E 枢轴 | q0.5 跨桶极差 0.06、q0.1 0.35、q0.9 1.17;主体塌缩、尾部漂移且不对称 | ✓ 主体枢轴;尾部非枢轴(如实写) |

## Fig 4 / τ*(全部实测,三值并列)
- MoE:ε̂(κ=5, 非地板)=1.76%;**Equalizer 交点 τ*=5.7**;闭合方程根 τ=4.0;
  λ_RL/c_static=14.8;λ_RL/c_train(早/中/晚)=3.8/1.8/1.4;c_static·τ*=0.89。
- 读法:以训练时尺度看,RL 最优 λ₊=2.3 对应 z≈1.4–3.8,与 Equalizer/闭合 τ*(4–6)
  同量级;静态 c 反推的 14.8 是 base 模型尺度上的数,两者差异来自"训练中 c 增长"。
- dense:ε̂=1.6%,τ*_eq=3.8,闭合 3.4,GPD ξ=0.07(近有界尾),高置信核外 P(>0)=0.72,q0.5 极差 0.001(枢轴极好)。

## Fig 5 触发动态(champion,1.54M token,step 0–84)——三条预言
1. 触发率:0.3% → 5.5% 平台(step~40 后),斜率 6e-4/步;**量级 ~ε 不成立**
   (陈旧占比仅 0.4–0.9%,触发率高一个量级);"缓慢上升"成立(前 40 步)后平稳。
2. 被截 token 置信度:**87–90% 在 u<0.1,p→1 端**;IcePop mask 人群集中在 p→0——
   **互补人群,预言全中,可进正文**(Fig 5b)。
3. 被截 \|log k\| 中位 0.089/0.098/0.100(早/中/晚)vs 该阶段 c_train·φ 中位
   0.008/0.013/0.011 → 比值中位 6.9/4.0/3.9,>5cφ 占比 72%/40%/41%。
   高置信端(地板区)截的是 epistemic 尺度 ✓;低置信端晚期带 ≈1.4σ_train,
   截入 aleatoric 尾(如实写)。
- lgrid_0 重做的截断表(替换 recipe_sigmatis 版):λ=2.3 截断率 3.9%,
  版本陈旧精度 3.0%(富集 5.1×)、召回 20%,核外精度(\|Z_train\|>5)49%;
  λ=6.7 时核外精度 93%。

## 各图 caption 的实测判读(一句)
- Fig 1(新鲜):两条线**同斜率**、常数因子分离——单一重尾尺度族,无加性层。
- Fig 1b(含陈旧/训练):高置信端出现 0.04–0.11 的残差层(cφ 的 10–100×);低置信端仍 ∝ φ。
- Fig 2:aleatoric 喇叭跨六个数量级收窄;正侧核外层在高置信端漂在喇叭之上;
  常数阈值(TIS/IcePop)只在 1−p→1 处触及;红带单侧、地板 0.0115 卡住高置信核外。
- Fig 3:主体分位塌缩(枢轴),GPD 尾 ξ=0.49,τ*=5.7 坐在主体→尾拐点,右侧质量 1.2%。
- Fig 4:过截偏差 H² 降、泄漏 D² 升,包络最小 τ*=5.7;clipping = bias equalization。
- Fig 5:见上三条。
