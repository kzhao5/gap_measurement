# log k_t | h_t 的真实条件分布:完整分析

> 数据:MoE 与 Dense 各 ~1,236 万 token(20k 条轨迹,DAPO-Math,T=1/top-p=1)。
> 记号:k_t = π_old(y_t|h_t) / π_infer(y_t|h_t),D = log k_t;h_t = (x, y_{<t}) 为上下文前缀;p_t = π_old(y_t|h_t)。
> 主图:`results/figs/V9_key_moe.png`(本文 §2 逐元素解读)。日期 2026-08-09。

---

## 0. 十行结论(TL;DR)

1. log k_t | h_t 不是单一分布,而是被一个标量索引的**分布族**:宽度 σ = c₁(1−p_t)(MoE c₁=0.195,dense c₁=0.046),**横跨六个数量级严格成立**;归一化后形状不随上下文变。
2. 形状**不是高斯**:对称核 + 近指数外点支 + 重尾(|z|>5 处密度比高斯高 3–5 个数量级)。
3. 逐 token 硬边界 −s_train ≤ D ≤ s_infer 恒成立;不对称随不确定度单调变化(高熵端负尾主导 2.8×)。
4. 每步恒等式 E[k_t|h_t] = 1 在 y-无关熵轴上逐桶验证通过(全局 E[K]=1.000009)。
5. TIS 的齐性假设、IcePop 的对称假设、KPop 的高斯形假设都被数据拒绝;**KPop 的 (1−p_t) 尺度几何被数据证实**——三方法各对一块。
6. token 求和形式(实践 loss)下:**校不校正 ≫ 怎么校正**(无校正 F=0.00225,任何校正 ≈0.00098–0.0010,方法间差在第三位小数)。
7. 序列乘积形式(你的推导)下差异被 T≈600 的连乘放大:**mask 类致命**(灭轨迹或上偏 1.33),**σ(h)-缩放 clip 全前沿最优**(F=0.0290,唯一超过 TIS 的族)。
8. 文献里"IcePop/KPop 优于 TIS"的真实含义:token 级、对比**无校正基线**、以训练稳定性为目标——与第 7 条不矛盾,是不同目标函数。
9. 推荐 M*:**log M = clip(log k_t, −a·σ̂_t, +b·σ̂_t),σ̂_t = max(c₁(1−p_t), 量化地板)**,a>b 不对称,阈值用经验 z 分位数而非高斯分位数。
10. 失配的物理来源:主体 = bf16 内核累积噪声(随 KV 长度 √n 增长);尾部 = MoE 路由翻转级联(剂量效应,负向);dense 上 ESS=67%、基本无需干预——**这本质上是 MoE 问题**。

---

## 1. 我们测了什么

同一份权重分别在 vLLM(推理引擎,bf16,采样方)与 HF/训练侧(teacher-forced 重算)下对**同一批已采样 token** 打分,得到逐 token 的 log π_infer 与 log π_old,差即 D = log k_t。附带测量:逐层 MoE 路由(专家 ID + gate margin)、fp32 重算、三组对照(C1 同引擎 prefill-vs-decode、C2 重算两遍、C3 逐层 hook)、全词表策略熵 H(π_old(·|h_t))。规模 2.47e7 token。数学上 π_old 与 π_infer 是**同一个策略**,所以 k_t≠1 全部来自数值实现差异——这是"训练-推理失配"的纯净测量,没有策略更新的混杂。

关键管线验证:C2 两遍重算 = 精确 0(测量地板干净);E[k_t] = 1.000009(重要性恒等式在 4 位小数闭合)。

## 2. 主图 V9 怎么读(`results/figs/V9_key_moe.png`)

**横轴(两联共享)**:token 不确定度 1−p_t,对数刻度,左端 = 模型近乎确定(p_t≈1),右端 = 高度不确定。

**上联 —— 真实条件分布本体**:
- **蓝色扇形带**:每个不确定度桶内 D 的分位数带,深→浅依次为中间 80%、98%、99.8% 的 token 真实落点。左边极窄(σ~1e-7)、向右张开(σ~0.15)——分布宽度完全被不确定度驱动。
- **黑线**:条件中位数,处处 ≈0——失配没有整体漂移,问题全在宽度与尾部。
- **灰色虚线(水平)**:常数阈值(TIS/IcePop 的门限几何,取 q99.9(|D|)=0.91)。它与不确定度无关,对图中 99.9% 的 token 高出分布宽度几个数量级——形同虚设,只"看见"最右端。
- **红色曲线**:±κ·c₁(1−p_t)(κ=8),即 KPop 的门限几何 / 我们建议的阈值。它跟着扇形走,每个 token 都被同样力度(z 单位)约束。

**下联 —— 尺度律(上联宽度的定量版)**:每桶实测 σ(蓝点)贴着 σ = c₁(1−p_t)(黑虚线)走过六个数量级;灰色水平线(常数阈值)只在最右端与之相遇。**这一联是全部设计结论的经验基础。**

Dense 版结构相同(c₁=0.046),唯一新元素:p→1 端 σ 触到 bf16 量化地板(~1e-6)后偏离直线——阈值在该端需要下限。

## 3. 真实分布的完整刻画(五条事实)

以下每条都可在 V8 系列图(`results/figs/V8_cond_{ptrain,entropy}_{moe,dense}.png`,六面板版,本文 §8 附导读)中找到对应面板。

**F1 尺度律(V8b/V9下联)**:σ_MAD(D | p_t) = c₁(1−p_t),MoE 六个数量级无系统偏离。备选的 Fisher 形 √((1−p)/p) 斜率整体错误。y-无关的熵轴上 σ(D|H) 同样单调(4 个数量级),证明这不是"条件在 y 上"的选择伪影。

**F2 形状族(V8c)**:z = D/σ 归一化后,不同不确定度桶的分布在 ±5σ 核内重合——条件律 ≈ 固定形状 × σ(h)。形状本身:对称核、重尾(高斯在 |z|=5 处已死,数据在 |z|=10 处仍有 1e-3 量级密度)、外点支近指数(V8f:|z|>3 后 log-CCDF 近直线)。工程含义:**z 空间全局校准一次即可**,不需逐 h 校准。

**F3 硬边界与不对称(V8a/V8e)**:log p ≤ 0 给出逐 token 恒等式 −s_train ≤ D ≤ s_infer(s = 对应侧 surprisal)。由此:高置信 token 的负偏差被封顶(train 轴视角)→ 正尾主导(3σ 尾比 0.15);高熵 token 负尾主导(MoE 2.8×,dense ~1.3×)——负向大偏差来自 MoE 路由级联(见 F5)。IcePop 假设的对称 ζ/2–ζ/2 与此不符。

**F4 均值与恒等式(V8d 两版)**:熵轴(y-无关)上 E[k_t|bin] = 1 逐桶 ±2SE 成立——你推导的前提被直接验证。p_t 轴上高熵端 E[D|bin] ≈ −σ_b²/2(对数正态均值结构,TIS 的均值假设在该端成立);低熵端被选择效应(winner's curse:被采样 token 是 π_infer 噪声下的赢家,重算平均吃亏)与量化地板主导。

**F5 物理来源(前期实验,RESULTS.md §1)**:主体 = bf16 矩阵规约顺序差异(C1 对照复现 bulk 的 94%;σ 随 KV 长度 √n 增长);尾部 = MoE 路由硬翻转级联(翻转率随层深单调升,邻层 lift 3.8×;固定不确定度带内每多一层翻转 mean D 更负,k≥8 层时 −7.1e-2);dense 无路由,σ 小 48 倍。零原子 w₀(D 精确=0):MoE 1.8%、dense 11.6%。

## 4. TIS / IcePop / KPop:同一框架下的三次近似

三方法都在做同一件事:对条件律 log k_t|h_t 假设一个参数形式,代入你的 MSE(M) 目标,解出最优修饰 M*。它们位于 2×2 设计空间 {门限几何:常数 vs σ(h)缩放} × {动作:clip vs mask}:

| | 假设的条件律 | 对的部分 | 错的部分 |
|---|---|---|---|
| TIS | N(−σ²/2, σ²),齐性 | clip 动作;−σ²/2 均值结构(高熵端真成立) | 齐性:常数阈在 z 单位下从 10⁶σ 变到 3σ |
| IcePop | 齐性高斯核 + 对称双侧 Exp(λ) | 外点支真实存在且近指数形 | 对称性(实测 0.15→2.8);齐性;mask 动作 |
| KPop | N(0, σ²(1−p_t)²) | **(1−p_t) 尺度几何(六个数量级证实)** | 高斯形(重尾使高斯分位数低估越界);mask 动作 |

"真实的不是分布吗"的答案:是分布——每个 h_t 一个;TIS 把整族坍缩成一个成员(齐性),KPop 本来就是一族(按 p_t 索引)。我们测出的就是这一族的完整描述:索引函数 σ(p_t)(F1)+ 固定形状 Z 的分位数表(F2)+ 不对称/边界(F3)+ 均值约束(F4)。**推导流程不变,换正确输入**:齐性高斯 → 常数 C*;尺度族 → C*(h) = κ·σ(h)(逐切片同一优化,κ 同一个数,因为形状不变);κ 由经验 Z(或 K_t 选型的 M4 参数形式)给出,闭式难看时数值 argmin(§5)。

## 5. 两种形式,两个前沿(经验计算,`analysis/mstar_frontier.py`)

你的 boxed 目标 F_n(M) = (E|W_m−W|)² + E[W_m²]/n 在 20k 轨迹上对任意 M 可直接算。两种形式:

**5a. 序列乘积形式(你手稿的 ĝ = E[(Π M(k_t))X])** — MoE,n=1024:

| 族(几何×动作) | 最优配置 | F | 关键病理 |
|---|---|---|---|
| 无修饰 | — | 0.0554 | log W: 均值−2.6, sd 2.6, **ESS=1.55%** |
| TIS(常数×clip) | c=1.0 | 0.0308 | |
| IcePop(常数×zero) | c=1.0 | 0.113 | c≤0.2 时 **100% 轨迹被置零** |
| IcePop(常数×skip) | c=1.0 | 0.157 | **skip 上偏**:E\|W_m−W\| 最高 1.33 |
| KPop(缩放×zero) | cz=20 | 0.178 | cz=3 时 99.9% 轨迹被置零 |
| KPop(缩放×skip) | cz=20 | 0.0332 | |
| **缩放×clip(建议)** | cz=20 | **0.0290** | 唯一超过 TIS 的族 |

两个结构性机理:(i) T≈600 且硬翻转污染 100% 轨迹时,"含坏 token 即灭轨迹"不可行;(ii) token 级 skip(把被删 token 的因子当 1)删掉的恰是**负漂移** token(E[D]<0 结构性成立),删后 W_m 系统性膨胀——这是所有"丢弃坏 token"方案的共同病灶,与阈值无关。clip 无此病。

**5b. token 求和形式(实践 loss ĝ = Σ M(k_t)A_t∇log π)** — F_tok = (E|M(k)−k|)² + E[M(k)²]/n:

| 方法 | F_tok |
|---|---|
| **无校正(M=1,朴素 GRPO)** | **0.00225** |
| 完全 IS / TIS clip / IcePop mask(松)/ KPop mask / 缩放 clip | 0.000983 – 0.001007 |

结论:token 级一步统计效率上,**校不校正是 56% 的差距,怎么校正是第三位小数的差距**。文献中 IcePop/KPop 的"更优"来自 (a) 对比无校正基线(这里完美复现),(b) 多步训练稳定性(静态 MSE 无法裁决,是本分析的边界)。机制补注:IcePop 删的 token 58–79% 是 k<1 的负尾(k≈0 在 k-加权 estimator 里自我沉默,删之近乎免费);KPop 的 z-门限在常用网格下主要删高置信、|D|≈0.005 的 token(σ 极小故 z 大)——数值上无关痛痒,其真正价值是门限几何。

**Dense 附注**:log W sd 仅 0.63,ESS=67%,无修饰 F=0.00146 已近最优——所有方法在松阈值下等价于不干预。**序列级失配问题在此规模上是 MoE 专属。**

## 6. 设计建议:M* 完整规格

$$\log M^*(k_t) = \mathrm{clip}\big(\log k_t,\; -a\,\hat\sigma_t,\; +b\,\hat\sigma_t\big),\qquad \hat\sigma_t = \max\big(c_1(1-p_t),\ \sigma_{\mathrm{floor}}\big)$$

| 规格 | 依据 |
|---|---|
| clip 不 mask | §5a 两病理;clip 保住恒等式的核,只动尾 |
| 门限 ∝ (1−p_t) | F1 六个数量级;每个 token 同等 z-力度约束 |
| 阈值用经验 z 分位数,非高斯 | F2 重尾:高斯分位数低估越界率 |
| a > b(负侧更宽) | F3:负支重且无界,正支被 s_infer 封顶 |
| σ_floor = 量化地板(bf16 ~1e-6) | dense 下联左端;防止把量化噪声当信号砍 |
| (a,b) 按目标批量 n 在前沿选 | n=1024 时 MoE 最优 z≈12–20;n 越小最优阈越紧;程序:`mstar_frontier.py` 扫网格 |
| c₁ 在线估计(滑动桶回归/EMA) | c₁ 是模型×引擎×精度的属性,训练中可能缓变 |
| dense/低失配场景:松阈值即可 | §5b dense:无修饰已近最优,别过度干预 |
| 长 CoT:σ̂ 可并入位置项 | 主体噪声随 KV 长度 √n 增长(F5) |

可选增强:残余负漂移的均值修正(高熵端 −σ̂²/2 项)或 self-normalization;若要论文闭式定理,用 K_t 选型叙事冠军 M4(截断 t 核 + 双侧指数支,参数已拟合:MoE a₊=6.65/a₋=4.62 等,`results/kt/`)作为 Z 的参数形式代入你的 MSE 推导。

### 6.1 算法框:σ-TIS(置信自适应缩放截断)

```text
校准(每个 模型×引擎 组合一次;训练中用 EMA 在线刷新)
  输入: 正常 rollout 的 (D_t = log π_old − log π_infer,  p_t = π_old(y_t|h_t))
        —— RL 管线本来就算这两个量,零额外开销
  c1     ← 分桶中位数回归: exp(mean_bin[log σ_MAD(bin) − log(1−p)(bin)])
  z 表    ← D_t / max(c1(1−p_t), σ_floor) 的经验分位数表(重尾,不用高斯)
  (a, b) ← 在 F_n 前沿上扫网格选定(analysis/mstar_frontier.py,按目标批量 n)

训练(逐 token,替换 loss 中 IS 权重位置的 k_t)
  σ̂_t ← max(c1 · (1 − p_t),  σ_floor)          # σ_floor = 量化地板(bf16 ~1e-6)
  w_t ← exp( clip( log k_t,  −a·σ̂_t,  +b·σ̂_t ) )   # a > b,不对称
```

命名:主推 **σ-TIS**(名字自带理论谱系:TIS 的推导在"齐性→尺度族"替换下的 h-条件化形式);备选 **CAST**(Confidence-Adaptive Scaled Truncation,品牌化)。为什么更好的三句话版:对无校正省 56% 一步 MSE;对 TIS,常数阈对 99.9% token 失明而缩放阈逐 token 等力度(序列前沿 0.0290 vs 0.0308,唯一胜出族);对 IcePop/KPop,保留其正确认知(外点支、(1−p) 几何)但换掉 mask 动作——mask 在序列形式下灭轨迹(100%)或上偏(1.33),clip 两病皆无。dense/低失配场景自动近似恒等变换(σ̂ 极小、阈值宽松)——不需要时不伤害。

## 7. 效度边界

- 每架构单模型、单域(数学)、T=1;高熵任务上 c₁ 与翻转率可能不同。
- F 的 bias 项是你推导的上界代理(B=1,X 的方向/相关结构未进入):绝对数字仅供族间排序;E[W] 尾部欠采样(20k 轨迹)使 bias 偏乐观,相对比较仍有效。
- 静态一步 MSE 捕捉不到多步训练动力学(崩溃、熵坍缩)——**最终裁决需真实 RL 对拍**(梯度余弦/熵曲线/reward),σ̂ 公式与阈值程序已是现成插件输入。
- 条件在 p_t(含 y_t)上时 E[k|bin]≠1 是选择效应,非恒等式失效;恒等式检验必须在 y-无关轴(已做,通过)。

## 8. 图件索引与导读

| 图 | 一句话 |
|---|---|
| **V9_key_moe**(主图) | 全部故事:真实条件分布(扇形)+ 尺度律(六个数量级)+ 两种门限几何的对质 |
| V8_cond_ptrain_{moe,dense} | 六面板细节版:a 扇形 b 尺度律 c 形状塌缩 d 均值结构 e 尾不对称 f 外点支形状 |
| V8_cond_entropy_{moe,dense} | 同上但 y-无关熵轴;d 面板 = E[k\|h]=1 恒等式逐桶检验 |
| V1–V7(RESULTS.md §1–2) | 失配测量原始判决图(池化分布/归因/路由机制) |

数据与脚本:`results/tokens_{moe,dense}.parquet`(逐 token 全量,含逐层路由位掩码);熵列 `recompute/*/recomp_*_bf16_ent.parquet`;脚本 `analysis/{cond_dist,key_figure,mstar_frontier,kt_model_selection}.py`。伴随文档:RESULTS.md(前期两实验的完整数值档案)。

---

## 9. RL 实战阶段(AReaL, 2026-08-10~11)

**管线**:AReaL 2.0.0 官方实现(TIS=clamp/ratio, IcePop=mask/[0.5,5], KPop=mask/binary_kl 2.0),Qwen1.5-MoE-A2.7B-Chat(即静态测量的模型),GSM8K,8×A100/臂,官方 MoE 配方(lr 3e-6, wd 0.01, clip 0.2/0.28)。σ-TIS 以 (λ₋,λ₊)=(1.0,2.3) 参数化(论文算子逐字实现)。修复史:sdpa×packed 跨序列污染(致首轮全废,FA2 wheel 修复,探针 med=0.000000/corr=0.9997);超时家族 5 处;详见 rl/patches/ 与记忆。

**结果**:
- 1-epoch 五臂:五方平手(末值 0.774–0.816,基线 0.72)。
- 3-epoch:fullis 0.831 / TIS 0.861 / IcePop 0.855 / KPop 0.859(87 步全稳);**σ-TIS 第 3 epoch 失稳(0.87→0.45)并于 79 步随 sglang KV 崩溃终止**。
- 触发分解(992 dumps):右触发 8.5→31.9%,被裁中位 |log k| 仅 0.0007(量化地板陷阱:p→1 带宽→0 裁掉纯噪声);左触发 2.4→10%,被裁中位 1.2–2.4(把真陈旧 token 权重抬回 1,反保守正反馈)。
- **算子修正(数据驱动)**:φ_t=max(1−p_t, ε₀), ε₀≈5e-3(带宽地板);默认 λ₋=∞(单侧化)。图:V12(1-epoch 五臂)、V13(3-epoch)。
- 定位结论:此 regime(lr 3e-6、T≤1k、滞后≤2)失配小到裸 IS 都稳——方法分化需更恶劣 regime(offpolicyness↑、长序列)。
