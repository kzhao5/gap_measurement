A 档（现有数据即可，全部 CPU）
E1 均值分解验收 + 位移地图（最优先）

问题：鞅模型的均值恒等式 μ_b = −σ_b²/2 − ε_b·E[e^ξ−1−ξ] 是否成立；位移质量住在哪。 表 E1a：按 (p 十分位 × pos 四段) 分桶，列 = bucket | μ̂_b | σ̂_b² | 拟合斜率贡献 | 残差 r_b | r_b 的 95% CI。 表 E1b：干净地层（低翻转桶）单独回归 μ 对 σ²，报 斜率 ± CI（判读：CI 含 −1/2 ⟹ 验收通过）。 判读：全部 r_b ≤ 0（补偿子非负性，鞅版本的独有预言）；显著 r_b < 0 的桶即位移地图——与剂量曲线的高翻转桶应重合。 工期：半天。

E2 标准化重拟合（s 坐标）

问题：各向异性解释掉多少"假尾"；残余尾是否翻转专属（P13 弱版）。 做法：s = (D + σ̂²/2)/σ̂(φ)，σ̂ = c₁(1−p)√(pos/pos₀)。 表 E2a：标准化前后对比，行 = {原始 D, 标准化 s}，列 = q999/MAD | 峰度 | 双成分 dBIC | 尾频@|·|>4。 表 E2b：残余尾 |s| > z_ψ 的组成：side | 尾内 token 数 | 其中硬翻转占比 | 对照全体占比 0.53。 表 E2c：s 坐标 GPD 重拟合 side | ξ̂_s | β̂_s | CI。 判读：E2a 中 s 的尾频与峰度大幅下降 ⟹ 假尾死亡；E2b 中翻转占比 ≫ 0.53 ⟹ 残余尾 = 真尾（跳跃支），格一/格三分界证实。 工期：半天。

E3 √pos 律去混杂

问题：宽度随位置增长是噪声钟（KV 累积）还是人口漂移（后期 token 更不确定）。 表 E3：行 = p 十分位桶；列 = 桶内 log σ̂ 对 log pos 的 斜率 ± CI（四个 pos 段）。 判读：桶内斜率 ≈ 0.5 ⟹ 钟是真的，s(pos)=s₀√pos 入模；≈ 0 ⟹ √pos 是伪影，算法删 pos 项。 工期：与 E2 共享桶结构，两小时。

E4 逐位置恒等式 + 增量自相关

问题：E[K|pos] = 1 是否逐位置成立；轨迹内增量是否独立（iid 工作近似受审）。 表 E4a：pos 网格上的 mean(e^D) | CI（判读：全程含 1）。 表 E4b：Var(Σ_t D_t) 实测 | Σ_t Var(D_t) | 差值 = 2ΣCov | 膨胀因子 ρ_infl。 表 E4c：自相关 τ ∈ {1,2,4,8,16,32} | Corr(D_t, D_{t+τ}) | 分层（翻转起点 vs 干净起点）。 判读：ρ_infl ≈ 1 ⟹ e^{Tσ²} 公式与 iid 近似获豁免；ρ_infl ≫ 1 ⟹ n_eff 再打折、Granularity 论证加强、scope 声明获定量因子；E4c 分层若显示翻转起点后自相关抬升 ⟹ 级联的时间轴指纹。 工期：半天（与 E5 同一 groupby 流水线）。

E5 序列级三个数（欠账清偿）

表 E5：arch | Ê[W] | Ê[W²] | ESS/n | 对照 e^{Tσ̂²} 预测。双架构。 工期：一小时。

E6 民间带审计

问题：[0.5, 5] 与我们的阈值各自的查准/查全。 表 E6：行 = {民间 [0.5,5], 推导带 [0.72,1.29]+帽 2, s 坐标带 |s|>z}；列 = 掩码率 | 掩码集内硬翻转占比 | 远尾(|D|>3.5)捕获率 | 恒等带内位移质量漏检率。 工期：两小时。直接进论文对比表。