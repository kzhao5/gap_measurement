# 训练-推理差距测量 + K_t 分布建模选型:实验结果与分析

日期:2026-08-09 · 集群:BYU dw(A100)· 代码:本 repo · 大文件:`~/nobackup/autodelete/gap_measurement/`(会被定期清除;小体积数值产物已拷贝到本 repo `results/`)

---

## 0. 实验设置

| 项 | 值 |
|---|---|
| 架构 | MoE(Qwen3-30B-A3B 级,24 个带硬翻转的 MoE 层)与 Dense 14B,各一 |
| 数据 | DAPO-Math prompts,每架构 20k 轨迹(G=8),T=1 / top-p=1(无截断,δ̂≡0) |
| 规模 | **2.47e7 token**(MoE 12,293,115 / Dense 12,389,115) |
| 主量 | D = log p_train − log p_infer(vLLM bf16 生成侧 vs HF 重算侧) |
| 矩阵 | 2 架构 × {bf16 重算, fp32 重算} + 对照 C1(同引擎 prefill-vs-decode)、C2(重算两遍)、C3(逐层路由 hook) |

**测量地板**:C2 两遍重算 = 精确 0(双架构)。hook 与内核路由 ID 在 margin>0 时一致率 100.00000%。

---

## 1. 主实验:差距底测量,判决 **H-var**(3/4)

### 1.1 预注册判决表(MoE)

| 检查项 | 观测 | 投票 |
|---|---|---|
| R1 混合成分+谱隙 | ΔBIC(1→2)=1.5M(强双峰)但 gap/σ = **−90**(翻转组与一致组主体重叠,成分与翻转标签不对应) | H-var |
| R2 P(硬翻转\|出界)/ε | 0.79 / 0.533 = 仅 1.5×,非 ≫ | H-var |
| R3 margin→翻转机制 | AUC=0.544;池化下翻转组中位 \|D\| 反而更小(见 1.4 混杂) | H-var |
| R4 尾部不对称 | n−=3.34M > n+=3.08M,p≈0 | H-flip(但见 1.5,应打折) |

### 1.2 核心数字

| 量 | MoE | Dense |
|---|---|---|
| σ_MAD(D) 全体 | 3.5e-3 | 7.4e-5(**48× 差**) |
| D 均值 / 中位数 | −4.2e-3 / ≈0 | −3.2e-4 / 0 |
| q99.9(\|D\|) / max | 0.91 / 13.0 | 0.18 / 0.87 |
| 原始任意层翻转 ε | 0.92(**平局饱和**:平均 1.9 个位级平局层/token) | — |
| 硬翻转 ε(margin>1e-3) | 0.533,特征 margin m*≈2e-3,100% 轨迹被污染 | — |
| 精确零原子 w₀ | 1.8% | 11.6% |
| C1 同引擎 prefill-vs-decode σ | 3.3e-3(≈bulk 94%) | 5.7e-5(≈77%) |
| fp32−bf16 重算 σ | 4.4e-3 | 4.6e-4(**是主差距的 6 倍**) |
| C2 | 精确 0 | 精确 0 |

### 1.3 主体 = 数值累积噪声(三条独立证据)

1. **C1 归因**:仅换 prefill/decode 内核路径即复现 bulk 的 94%/77% —— 近因是批量矩阵规约顺序,不是"训练框架 vs 推理引擎"。
2. **位置 √n 缩放**:MoE σ_MAD 从 pos<256 的 2.0e-3 → pos∈[1k,4k] 的 5.9e-3(≈√(4096/256));ε_hard 不随位置涨(0.54→0.49)。噪声随 KV 长度长,翻转不长。
3. **dense 对照**:无路由架构同结构(含负均值),小 48 倍。

### 1.4 翻转的真实伤害被置信度混杂遮蔽(补充分析)

翻转层数 k 与 token 置信度强相关(k≥8 组 76% 是 p>0.99 token,k=0 组仅 33%)。池化下"k 越大 median|D| 越小"是混杂假象。**固定不确定带(−3<logp<−0.5,267 万 token)内,剂量效应单调且陡**:

| k(硬翻转层数) | med\|D\| | mean D |
|---|---|---|
| 0 | 5.9e-2 | −6.8e-3 |
| 1 | 7.2e-2 | −1.1e-2 |
| 2 | 8.5e-2 | −1.6e-2 |
| 4 | 1.1e-1 | −3.0e-2 |
| ≥8 | 1.6e-1 | **−7.1e-2** |

配套机制证据:逐层硬翻转率随深度单调上升(0→0.095,l=0..23);邻层条件提升 lift 均值 3.8×、最高 10×(级联/共因成串)。

### 1.5 负不对称主要是选择偏差(R4 那票应打折)

无路由的 dense 均值同样显著为负(−3.2e-4 ≈ 4σ_MAD);MoE 无翻转 token 均值也为 −3.3e-3。被采样 token 是 p_infer 噪声实现下的"赢家",重算换噪声必然平均吃亏(winner's curse)——E[D]<0 是结构性的,与翻转无关。故 H-var 判决比 3/4 显示的更扎实;但 log-ratio 负期望对 RL 有独立意义(系统性低估采样 token)。

---

## 2. K_t 分布建模选型(协议 kt_model_selection_protocol.md)

实现:`analysis/kt_model_selection.py`(预注册操作定义 P1–P14 见文件头;关键:尾轴取 K 轴幂律 = D 轴指数尾)。四套输出:`kt_{moe,dense}_{traj,time}.md/.json`。

### 2.1 资格审查(表0)

- **经验 E[K] = 1.0000,CI [1.0000, 1.0001],双架构** —— 重要性比恒等式 4 位小数闭合,测量管线自洽。
- 过审:{M1, M2, M4, M6, M7}。**M3 双门阵亡**(E[K]=∞ + 4 格 Markov 违反,T3 相对误差 ~1e10 ——"重尾强制截断"展品)。**M5 挂 E[K] 门**(ξ̂₊≈0.15>0 ⇒ ∞)但过 Markov 门 —— 最好的尾校准器蕴含发散均值,插件层必须显式截断。
- 修复记录:M4 的 implied E[K] 首轮 0.88/0.97 为 trapz 尖峰积分伪影,分位空间积分重算 = 1.0000,双架构 PASS。

### 2.2 双冠军(终判)

| | MoE | Dense |
|---|---|---|
| **叙事冠军**(T4 NLL,过审者中) | **M4**(NLL −3.31, PIT-KS 0.093) | **M4**(NLL −5.65, PIT-KS 0.064) |
| **算法冠军**(T3 主 + T1 次) | M5(time 切分;traj 切分 M6 仅凭 σ_L 列险胜) | **M5**(0.044,断层) |
| T1 尾校准最优 | M5(0.016,四套全胜) | M5(0.044) |

**M5 的 σ_L 脚注(真实局限,非伪影)**:2048 桶经验直方图体部分辨不出近零量化梳处的 cdf(0),中心敏感泛函(σ_L)误差可达 80% 且随切分抖动;尾部泛函(m*、C_n、尾分位)四套全部压制。**操作结论:尾件用 M5(每侧 u/ξ/β + 显式截断),中心量直接用经验值,论文形状章用 M4。**

### 2.3 三个预注册裁决

1. **Hill 平台不成立**(MoE 0/8 相邻重叠,dense 1/8)。MoE â₊ 随 ψ 从 5.1 爬到 9.6;dense 非单调(27→46→27)且 ξ̂ 跨零变号,大 ψ 处 ξ̂<0(近有界端点——与"无级联、纯累积噪声"一致)。按规则 4:只报 u 条件尾参数,不加冕普适尾指数。
2. **椭圆:强形式不成立,弱形式强成立**。6a 桶内正态性 100% 拒绝(2 万样本/桶 + bf16 量化梳 ⇒ 功效过剩,引用时用弱形式);**6b:σ(桶)=c·(1−p) 拟合 R²=0.938(MoE)/0.984(dense)**,物理噪声形略胜 KPop-Fisher 形(0.925/0.974)。6c 等效 ν 与 M4 ν̂ 不相容(注意 M4 ν̂ 压在 0.05 优化边界)。
3. **方向档案(表7)**:MoE **ε̂₋/ε̂₊ = 1.789 [1.77,1.81]**,â₋=4.62 < â₊=6.65(左尾更重);dense 仅 **1.139 [1.13,1.15]**。方向性不对称干净、被 MoE 放大 ~6 倍。

### 2.4 协议预期 5 落空(真发现)

M1 在 dense 上**没有**翻身(T1 误差 0.73,中心 QQ 斜率 168)。架构差异的正确定量陈述:**非高斯性(量化梳+重心峰)是引擎算术通性;MoE 改变的是尾指数(5–10 vs 26–46)、端点(发散型 vs 近有界)与不对称度(1.79 vs 1.14)**。另:M7 条件 NLL 在 MoE 上爆正(+6143)——(p, margin, pos) 桶解释不了尾 token,与 1.4 的"尾=翻转级联剂量效应"互证。

---

## 3. 对 masking/IcePop 替代方法设计的操作含义

1. **flip 本身不能当 mask 判据**(ε=0.533,100% 轨迹污染)。
2. **ratio 门限与数据结构相容**,但翻转伤害集中在不确定 token(梯度承载者)——设计时按 token 熵分层评估被砍 token 构成,防止砍掉学习信号。
3. **E[D]<0 是结构性的** ⇒ 偏好保均值的截断重要性采样(TIS/CISPO 类)而非硬丢弃。
4. **最便宜的工程修复在 router**:m*≈2e-3 在 bf16 分辨率附近 + 位级平局饱和 ⇒ router fp32 + 一致平局决胜可能消掉大部分翻转,值得单独消融。
5. **长 CoT 风险更高**:σ 随上下文 √n 增长,门限可随位置放宽或按轨迹归一。
6. 插件数值:MoE ψ=1% 档 u₊=0.258/u₋=0.324,â₊=6.65/â₋=4.62,ξ̂₊=0.146/ξ̂₋=0.275(全部带 bootstrap CI,见 kt_moe_traj.json)。

---

## 3.5 V8:log k_t | h_t 真实条件律 vs TIS/IcePop/KPop 的假设律

h_t = (x, y_{<t}) 为上下文前缀(用户推导);k_t = π_old(y_t|h_t)/π_infer(y_t|h_t)。三方法的假设律:TIS = N(−σ²/2, σ²) 齐性;IcePop = 齐性高斯核 + 对称双侧 Exp(λ) 外点支;KPop = N(0, σ²(1−p_t)²),p_t=π_old(y_t|h_t)。条件投影轴:p_t(KPop 变量,`--cond ptrain`)与 y-无关的策略熵 H(π_old(·|h_t))(`--cond entropy`,由 `slurm/entropy.sbatch` 重算,~3 分钟/shard)。图:`results/figs/V8_cond_{ptrain,entropy}_{moe,dense}.png`,脚本 `analysis/cond_dist.py`。

| 假设 | 实测 | 面板 |
|---|---|---|
| 齐性(TIS/IcePop) | 否:σ(D\|p_t) 随 (1−p_t) 变化 6 个数量级;σ(D\|H) 变化 4 个数量级 | b |
| KPop 尺度律 σ∝(1−p_t) | **是**,MoE 上 1e-7→1e-1 全程贴合;dense 在 p→1 端触 bf16 量化地板(~1e-6)后偏离 | b |
| 高斯形(TIS/KPop) | 否:桶内近似尺度族但尾远重于高斯(\|z\|>5 处密度高 3-5 个数量级) | c |
| IcePop Exp(λ) 外点支 | 形状近似成立(高熵桶 \|z\|>3 后 CCDF 近直线)但 λ 依赖 h,低熵桶无左支 | f |
| IcePop 对称 ζ/2-ζ/2 | 否:3σ_b 尾比在 p_t 轴上从 0.15(高置信,正尾主导)单调升到 2.8(高熵,负尾主导) | e |
| KPop 均值=0 / TIS 均值=−σ²/2 | 高熵端 E[D\|bin]≈−σ_b²/2(TIS 均值结构成立);低熵端被选择效应与量化地板主导,均值可正 | d(ptrain) |
| **每步恒等式 E[k_t\|h_t]=1** | **在 y-无关熵轴 36 桶内全部 ±2SE 成立**(全局 E[K]=1.000009/1.000007) | d(entropy) |
| 支撑几何 | 硬边界:D ≥ −s_train 且 D ≤ s_infer(逐 token 恒成立),塑造全部不对称几何 | a |

**净结论**:三方法中 KPop 的条件尺度假设最贴近真实律;其均值/形状假设的偏离(负偏、重尾)与齐性方法的 h-无关假设的偏离,都可用本表数据直接量化进 M* 的 bias-variance 目标(推导中的 (E\|W_m−W\|)² + E\|W_m²\|/n 可在 20k 条轨迹上对任意 M 经验评估——tokens parquet 已含全部 D_t)。

## 3.6 M* 经验前沿(boxed 目标 F_n = (E|W_m−W|)² + E[W_m²]/n,n=1024,20k 轨迹)

脚本 `analysis/mstar_frontier.py`。序列权重 W=Πk_t:MoE log W 均值 −2.61、sd 2.62、**ESS/n=1.55%**(裸序列 IS 在 T≈600 已死);dense ESS 67%(基本没问题,no-mask F=0.00146 已近最优)。MoE 各族最优 F:

| 族(几何×处理) | 最优配置 | F_1024 | 备注 |
|---|---|---|---|
| no-mask | — | 0.0554 | 基线 |
| TIS(常数×clip) | c=1.0 | 0.0308 | |
| IcePop(常数×zero) | c=1.0 | 0.113 | c≤0.2 时 100% 轨迹被置零 |
| IcePop(常数×skip) | c=1.0 | 0.157 | skip 上偏:E\|W_m−W\| 最高 1.33 |
| KPop(缩放×zero) | cz=20 | 0.178 | cz=3 时 99.9% 轨迹被置零 |
| KPop(缩放×skip) | cz=20 | 0.0332 | |
| **缩放×clip(OURS)** | cz=20 | **0.0290** | 全前沿最优;cz=12 为 0.0327 |

三条机理结论:(1) T≈600 下任何"含坏 token 即整轨迹置零"都不可行;(2) token 级 skip(实践中的 masking)因删除的是**负漂移** token 而系统性抬高 W_m——结构性上偏;(3) clip 无此病,且 σ(h)-缩放的 clip 帧定最优前沿。**建议 M\*:log M = clip(log k_t, −a·σ̂_t, +b·σ̂_t),σ̂_t = max(c₁(1−p_t), 量化地板),(a,b) 不对称按目标批量 n 在前沿上选。**注意:F 的 bias 项是上界代理(B=1,未含 X 相关结构),绝对值仅供排序;E[W] 尾部欠采样使 bias 数字偏乐观,族间比较仍有效。

## 4. 效度边界

- 每架构单模型、仅数学域、T=1 贪心邻域(低熵;高熵任务 ε 可能不同)。
- 硬翻转阈 1e-3 系发现平局饱和后事后选定(偏离已记录)。
- R1 的 ΔBIC 双峰可能部分来自 D=0 量化尖峰(谱隙门为此存在并否掉了对应关系)。
- R4 有选择偏差混杂(1.5);K_t 表6a 有功效过剩问题(2.3)。
- K_t 预注册操作定义 P1–P14 中 [DEV] 标记处为协议未明说的实现选择,可否决重跑。

---

## 5. 产物索引

| 产物 | 路径 |
|---|---|
| 本总结 | `RESULTS.md`(repo) |
| 主实验统计/报告 | `results/stats_{moe,dense}.json`, `results/report.md`(源:nobackup analysis/) |
| 图 V1–V7 双架构 | `results/figs/`(PNG+PDF) |
| K_t 八份数表 | `results/kt/kt_{moe,dense}_{traj,time}.md/.json` |
| **逐 token 主数据表**(log K_t=D 及全部 logp 变体+翻转位+margin,2.47e7 行) | `results/tokens_{moe,dense}.parquet`(644M,已拷回 home)|
| 更底层分阶段 parquet(gen 2.1G / recompute 4.3G / c1,含逐层 route+margin) | `~/nobackup/autodelete/gap_measurement/{gen,recompute,c1}/` ⚠️ 会被清除 |
| 未保存 | 全词表分布/逐 token 熵 h_t(只存了被选 token 的 logprob;需 h_t 须在 recompute 加一列重跑,~1h GPU/架构)|
| 代码 | `src/`(生成/重算/对照)+ `analysis/`(build_dataset, stats_report, figures, kt_model_selection)+ `slurm/` |

复现:`scripts/submit_full.sh` 一键(环境坑:FIPS→`OPENSSL_CONF=/dev/null`;dw-2-4 GPU 隔离损坏须排除;dw87 QOS 上限 3 天且长时限预扣排队额度;Slurm 日志写 nobackup 防 home 配额)。
